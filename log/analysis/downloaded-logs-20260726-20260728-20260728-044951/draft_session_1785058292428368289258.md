# セッション遅延分析: `1785058292428368289258`

| 項目 | 値 |
|------|-----|
| 分析対象 | `session_conversations.sessions[]` の当該オブジェクト + `sessions/1785058292428368289258.md` |
| ユーザー入力 | 「ロキソニンとイブの画像見せて」 |
| channel | web |
| 遅延ターン | ターン 2（counseling_detail） |
| pipeline `total_ms` | **351,778 ms（約 351 秒）** |
| breakdown 最終ステップ | `safety_gate_done` = **9,110 ms（約 9 秒）** |
| **計測ギャップ** | **約 342 秒**（351,778 − 9,110） |
| trace_id | `9c808e37-6e41-45c7-9bb5-508e0a8dacc2` |
| POST 開始 | `2026-07-26T09:31:37.685185Z` |
| 応答完了 | `2026-07-26T09:37:29.463273` |

---

## 結論（遅延の根本原因）

**351 秒の大半（約 340 秒）は、`safety_gate_done` 以降の未計測区間で HTTP リクエストがブロックされ続けた時間である。**

計測上の「ギャップ」は主に **PIPELINE_PERF の breakdown が `safety_gate_done` で打ち止め**になっているため見える化されていないが、実際の wall-clock でも POST 開始から約 9 秒でセーフティゲート通過後、**約 6 分弱レスポンスが返らなかった**。

根因として最も有力なのは次の組み合わせ:

1. **同一ユーザー入力に対する重複・並行リクエスト**（旧セッション `1785057159607653291042` 上の 338 秒リクエストと並行）
2. **インスタンス飽和**（09:33:36 頃の静的アセット大量 429、同日朝の SSE worker 180s タイムアウト先例）
3. **`safety_gate_done` 以降の medicine_qa 経路が計測対象外**のため、ボトルネック箇所（executor 待ち / DB / 応答組み立て）がログ上特定不能

同一メッセージの再試行（セッション `1785058927087582422001`、09:42 POST）は **12 秒**で完了しており、**ロジック自体は通常 fast path（製品画像 QA）で処理可能**である。遅延は機能バグというより **当該時間帯の実行環境・並行負荷**が主因。

---

## 340 秒ギャップの説明

### 1. 計測構造（なぜ breakdown が 9 秒で止まるか）

`PIPELINE_PERF` の `total_ms` は `bind_pipeline_perf` 開始（POST 処理入口）から `log_pipeline_perf`（`handle_chat_post` の `finally`）までの **実経過時間**。

一方 `breakdown` は `mark_pipeline_step()` が呼ばれた地点のみ記録される。`chat_post_pipeline.py` では:

- 最後の routinely な計測: **`safety_gate_done`**（L399 付近）
- その後の medicine_qa / orchestrator / DB 保存など: **ステップ未登録**

そのため **`total_ms` − `safety_gate_done` ≈ 342 秒** が「未分解の暗黙区間」として現れる。これは計測バグではなく **計測カバレッジの欠落**。

### 2. 実時間軸（wall-clock）

| 時刻 (UTC) | イベント |
|------------|----------|
| 09:31:37.685 | POST 開始（trace `9c808e37`） |
| 09:31:42〜45 | triage LLM stage1/2（計 ~2.9s） |
| 09:31:45.412 | triage 完了（Other / general_other, confidence 0.89） |
| 09:31:46.791 | gate → **Physical / medicine_qa**（shadow mismatch） |
| 09:31:46.792 | `SessionOps phase=triage` |
| **09:31:46 〜 09:37:29** | **約 342 秒の無応答区間（breakdown 未計測）** |
| 09:37:29.463 | `PIPELINE_PERF` 出力 + counseling_detail 応答 |

`safety_gate_done`（~9.1s）時点ですでにルーティング判定は完了。**遅延の本体はゲート通過後の応答生成〜返却まで**。

### 3. LLM コストが示す処理内容

当該リクエストに紐づく LLM 呼び出しは **triage の 2 回のみ**（合計 ~2,900 ms、¥0.20 程度）:

| path | model | latency |
|------|-------|---------|
| llm_triage.stage1 | gpt-5.4-mini | 1,439 ms |
| llm_triage.stage2 | gpt-5.4-mini | 1,461 ms |

**`medicine_qa/focus_llm` も `medicine_response_builder.chat_context.answer_stream` も記録なし。**

製品画像 QA は `_try_fast_product_image_qa_response` により **LLM 省略の fast path** が存在する。正常時は数秒以内のはず。340 秒は **LLM 推論時間では説明できない** → **スレッド / DB / インスタンス待ち、または fast path 到達前のブロック**が疑わしい。

---

## セッション概要

### session_conversations より

- **ターン数**: 2（grade: good）
- **input_labels**: `image_generation`（両ターン）
- **ターン 1**: `conversation_history` 由来、`sage_qa` プレースホルダー的応答、タイムスタンプなし
- **ターン 2**: `counseling_detail`、09:37:29 応答、製品画像 HTML 付き最終回答
- **routing / timing フィールド**: 空（分析 JSON 側未結合）

### 最終応答（正常完了）

ロキソニンS・イブのパッケージ画像（`images.yutok.dev/otc/` CDN）と成分説明テキスト。機能的には期待どおり。

---

## 並行リクエスト（重要コンテキスト）

同一ユーザー入力「ロキソニンとイブの画像見せて」に対し、**重なり合う長時間 POST が 2 本**存在する。

| セッション | POST 開始 | PIPELINE_PERF | total_ms | 備考 |
|-----------|-----------|---------------|----------|------|
| `1785057159607653291042`（旧） | 09:29:54 | 09:35:32 | 338,327 ms | 同一 trace 系統上、先に開始 |
| **`1785058292428368289258`（本セッション）** | 09:31:37 | 09:37:29 | **351,778 ms** | 新セッションで再送と推定 |

chat_flow 上、trace `9c808e37` の `session_id` フィールドは旧 SID を指すが、`pipeline_perf.sid` は **`1785058292428368289258`**。**セッションローテーション（`/new_session` 等）後も同一 HTTP 処理が継続**した、または **旧リクエスト完了前に新 SID で再 POST** した UX と整合する。

旧セッション側も breakdown は `safety_gate_done` ~9s で止まり、**同型の ~330s 暗黙区間**を持つ。

---

## ルーティング

| 項目 | 値 |
|------|-----|
| triage | **Other / general_other**（confidence 0.89） |
| gate 判定 | **Physical / medicine_qa**（layer1_medicine_qa, confidence 0.94） |
| mismatch | `gate_improvement`（triage と gate 不一致） |

比較: 09:42 の成功リトライ（`1785058927087582422001`）は triage **Ask / medication_identification**（0.98）で **12 秒**完了。カテゴリ差はあるが、いずれも gate は medicine_qa。

---

## インフラ・周辺信号

| 時刻 | 信号 | 意味 |
|------|------|------|
| 09:33:36 | 静的 JS/CSS への **429** 多数 | Cloud Run インスタンス / レート制限下の負荷 |
| 06:09〜06:13（同日） | `SSE chat worker timeout after 180s` ×4 | 同環境でワーカー長時間ブロック先例 |
| revision | `medicine-recommend-dev-00205-xk4`（429 時） | 当該時間帯のデプロイリビジョン |

当該 SID 固有の SSE timeout ログはエクスポートに無いが、**インスタンス混雑パターンは同日確認済み**。

---

## 根本原因の整理

| 要因 | 説明 | 確度 |
|------|------|------|
| **計測カバレッジ不足** | post-gate 340s が breakdown に現れない | 確実 |
| **並行長時間 POST** | 旧 SID 338s + 本 SID 351s が重複 | 高 |
| **executor / インスタンス飽和** | 429・同日 SSE timeout 先例、LLM ほぼ無しで 340s | 高 |
| **medicine_qa fast path 未到達 or 後段ブロック** | LLM 呼び出し無し、最終的には画像応答成功 | 中 |
| triage 誤分類 alone | 再試行 12s 成功のため単独原因ではなさそう | 低 |

---

## 推奨アクション

### 計測（優先）

`safety_gate_done` 以降に最低限以下を `mark_pipeline_step` 追加:

- `medicine_qa_enter` / `medicine_qa_done`
- `orchestrator_enter` / `orchestrator_done`（agent 有効時）
- `response_persist_done`

`total_ms` と breakdown 最終値の **delta を `uninstrumented_ms` としてログ出力**すると、同種 incident の triage が容易になる。

### 運用・性能

- 同一入力の **重複 POST / セッション切替再送**時に inflight 可視化（旧 SID job 未完了警告）
- `CHAT_STREAM_TIMEOUT_SEC`（180s）超過後もワーカーが走り続ける orphan worker パターンの監視強化
- 340s 級 incident 発生時に **429 / executor queue depth / Neon latency** を相関ダッシュボード化

### 品質（副次）

- triage `Other/general_other` → `Ask/medication_identification` への改善（画像依頼）— 遅延主因ではないがルーティング品質向上

---

## 参照データ

- `sections/pipeline_perf.json` — sid `1785058292428368289258`, total_ms 351778.45
- `sections/chat_flow.json` — trace `9c808e37-6e41-45c7-9bb5-508e0a8dacc2`
- `sections/misc_signals.json` — 09:31:46 routing, 09:37:29 PIPELINE_PERF
- `sections/llm_cost.json` — triage のみ 2 calls
- `sessions/1785058292428368289258.md` — transcript（breakdown 未結合）
- 比較: `sessions/1785057159607653291042.md`（338s 並行）、`1785058927087582422001`（12s 成功）

---

*生成: ログ分析 draft（`downloaded-logs-20260726-20260728-20260728-044951`）*
