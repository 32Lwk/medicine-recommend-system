# AWS Log Analysis Report

## メタデータ

| 項目 | 値 |
|------|-----|
| プラットフォーム | **aws** |
| ソースファイル | `log/raw/downloaded-aws-logs-20260725-20260725-20260725-024329.json` |
| Log Group | `/ecs/medicine-recommend` |
| Region | `ap-northeast-1` |
| ECS Service | `medicine-recommend`（staging） |
| 期間 | 2026-07-25T00:49:48Z ～ 2026-07-25T02:43:28Z（約 1 時間 54 分） |
| エントリ数 | 2,983 |
| ログストリーム | 4 本（01:52 UTC ローリング入替） |
| 重大度 | ERROR 4 / WARNING 296 / INFO 2,244 / DEBUG 439 |
| セッション数 | counseling 2 / trace-only 0 / chat_flow トレース 2 |
| 解析 stem | `downloaded-aws-logs-20260725-20260725-20260725-024329` |

---

## エグゼクティブサマリー

AWS ECS ステージング（`aws.medicine.yutok.dev`）の差分ログ 2,983 件（約 2 時間）を解析した。**ユーザー向け 5xx は 0 件**。HTTP 4xx 279 件の 98%（273 件）は 01:34 UTC の bot スキャン burst（`.env` / `.git` / `phpinfo.php` 等）であり、可用性への直接影響なし。

**インフラ**: 01:52 UTC に ECS ローリングデプロイ（新タスク 2 起動 → 01:59 UTC 旧タスク SIGTERM）。CloudWatch ERROR 4 件はすべて Gunicorn SIGTERM（想定内ノイズ）。DB/Neon は SSL 切断を 1 試行で自動復旧。`/helth` typo 404 が 6 件（外部モニタ設定ミス疑い）。

**性能**: Web 2 ターンとも E2E **~8.3–8.7 s**（前日 24h 中央値 13.6 s より短い）。triage-only 経路で LLM は stage1 のみ（**0.19 JPY / 2 calls**）。ボトルネックは triage 非 LLM 区間（~1.9 s）と safety_gate 後段（~1.0–1.4 s）。

**会話品質**: 2 セッション / 実質 1 ターンずつ。入力は同一「ロキソニンって眠くなる？」。ヒューリスティック **needs_improvement 2/2**（`side_effect_qa_mishandled` critical ×2）だが、**Wave B LLM 再判定は両セッション ok** — ルーティング・応答とも正常で **false positive**。前日 24h レポートで報告した systemic bug（副作用 Q&A → escalation 誤解）は**本窓口では再現せず**。

**連携**: LINE webhook 0 件（Web のみ）。OpenAI API 正常（200 ×12）。Translate/Polly ValidationException 4 件（01:59 UTC、部分フォールバック成功）。

---

## インフラ・エラー（infra_errors）

### HTTP 異常

CloudWatch には GCP 型 `httpRequest` なし。テキスト解析で **279 件（5xx: 0）**。

| ステータス | 件数 | 主因 |
|-----------|------|------|
| 404 | 273 | 01:34–01:35 UTC スキャン burst（267 件）+ `/helth` typo（6 件） |
| 422 | 4 | スキャナ `POST /`（Form 欠落） |
| 400 | 2 | **Polly 外向き API**（インバウンド HTTP ではない） |

**01:34 UTC スキャン burst**: `172.31.41.145` から `.env` 系 ~206 件、`/phpinfo.php` 等 ~20 件、`/.git/config` 1 件。ALB WAF で block / rate-limit 推奨。

**`/helth` typo**: 02:30–02:41 UTC に 6 件。正しいエンドポイントは `GET /health`（本窗口 1,384 件 200）。

### ECS デプロイ境界

`deploy_revision.json` は task definition 未検出。ログストリーム 4 本で推定:

| フェーズ | 時刻 (UTC) | 内容 |
|---------|-----------|------|
| 旧タスク稼働 | 00:49:48 ～ 01:59:55 | 2 ストリーム |
| 新タスク起動 | **01:52:18** / **01:52:49** | Gunicorn 起動 ×2 |
| 旧タスク停止 | **01:59:50** / **01:59:55** | SIGTERM ×4 → Master shutdown |

オーバーラップ約 7 分 40 秒。502/503 なし — **benign deploy noise**。

### テキスト ERROR

| 時刻 (UTC) | 内容 | 深刻度 |
|------------|------|--------|
| 01:59:50 / 01:59:55 | Gunicorn `Worker was sent SIGTERM!` ×4 | 🟢 デプロイノイズ |

`text_errors` 0 件 — DB プール枯渇等の実障害シグナルなし。

---

## 性能・コスト（performance_cost）

| チャネル | 件数 | min | avg | median | max |
|----------|------|-----|-----|--------|-----|
| web | 2 | 8.3 s | **8.5 s** | **8.5 s** | 8.7 s |

**フェーズ分解（2 件共通パターン）**

| フェーズ | ms 目安 |
|----------|---------|
| 前段（post_start → security） | ~1,150 |
| security_phase | 240–496（avg 368） |
| **triage 本体** | **~3,762–4,022**（LLM ~1.9–2.0 s + **非 LLM ~1.9 s**） |
| safety_gate 完了 → 応答完了 | **~976–1,361** |

**LLM コスト**: **2 calls / 0.192 JPY**。path は `llm_triage.stage1` ×2（gpt-5.4-mini）のみ。Concierge / intent_router / 推奨 LLM は 0 件。

| session_id（末尾） | cost_jpy |
|-------------------|----------|
| …8821 | 0.0964 |
| …9915 | 0.0956 |

**前日 24h 比較**: 本窓口は低トラフィック（n=2）の triage-only スナップショット。Concierge・推奨経路ボトルネックは未観測。

---

## 会話品質（conversation_quality）— 横断サマリ

| session_id（末尾） | channel | 記録ターン* | heuristic grade | **LLM grade** | 要約 |
|-------------------|---------|------------|-----------------|---------------|------|
| …8821 | web | 2 | needs_improvement | **ok** | ロキソニン眠気 Q&A — 正常。heuristic false positive |
| …9915 | web | 2 | needs_improvement | **ok** | 同一入力・同一応答の再試行。同上 |

\* `conversation_history` + `counseling_detail` デュアルソースにより 1 発話 = 2 ターン記録。

**共通フロー**: triage `Ask` (conf 0.98) → shadow `Physical → medicine_side_effect_qa` (gate) → `sage_qa` / `medicine_qa` カード応答。

**遅い trace**: 2/2 が ≥8 s（100%）。triage + safety_gate が支配。LLM 本体は ~2 s のみ。

### 意図ずれレビュー

| 時刻 (UTC) | session | ユーザー入力 | issue_type（heuristic） | heuristic | **LLM 判定** |
|------------|---------|--------------|------------------------|-----------|-------------|
| 02:32:39 | …8821 | ロキソニンって眠くなる？ | side_effect_qa_mishandled | 🔴 critical | **false positive — ok** |
| 02:42:42 | …9915 | ロキソニンって眠くなる？ | side_effect_qa_mishandled | 🔴 critical | **false positive — ok** |

**false positive 根因（Wave B 確定）**:
1. `turns[].routing.sub_route` 未伝播 → triage subcategory（`drug_side_effect` / `side_effects`）と `medicine_side_effect_qa` を不一致扱い
2. 副作用 Q&A 標準免責文（「眠気」＋「医師に相談」）が `looks_escalation` を誤トリガー
3. shadow mismatch **0/2** — ルーティング自体は正常

**前日比較**: 24h レポート（`…3443` / `…2059`）では LLM も **poor** と判定した escalation 誤解があった。本窓口 2 件は gate 経由で正しく副作用 Q&A 応答 — **改善または再現条件の差**の可能性。

---

## 連携・その他（integrations）

| 領域 | 所見 | 深刻度 |
|------|------|--------|
| LINE webhook | 0 件（Web のみ） | 🟢 |
| Neon DB | SSL 切断 → 全件 1 試行で再接続。01:52 以降 pool `max: 10 → 20` | 🟢 |
| `channel_binding=require` | 起動 WARNING 5 回（自動除去） | 🟡 |
| OpenAI API | 200 OK ×12。エラーなし | 🟢 |
| Translate / Polly | ValidationException 4 件（01:59 UTC）。Polly fallback 成功 | 🟡 |
| Gunicorn/ECS | 再起動 2 回・SIGTERM 4 件（01:52–01:59 UTC） | 🟢 |
| Emergency | 検出 2 回・すべて「検出なし」 | 🟢 |
| 外部スキャナー | `.env` / `.git` プローブ → 404 拒否 | 🟢 |

---

## セッション別評価

### セッション: `1784946740773995708821` — **ok**（LLM）

**概要**: 「ロキソニンって眠くなる？」副作用 Q&A。shadow `medicine_side_effect_qa` gate 解決 → `sage_qa` カードで眠気に関する妥当な回答。

| # | ユーザー入力 | 応答（抜粋） | E2E (ms) | LLM 判定 |
|---|-------------|-------------|----------|----------|
| 1 | ロキソニンって眠くなる？ | sage_qa（プレースホルダ） | 8,345 | 🟢 |
| 2 | ロキソニンって眠くなる？ | ロキソニンＳでは強い眠気は主要副作用に含まれない旨… | 8,343 | 🟢 |

**LLM コスト**: ¥0.0964（triage stage1 のみ）

---

### セッション: `1784947344525367619915` — **ok**（LLM）

**概要**: 約 10 分後の同一入力再試行。応答本文・ルーティングとも …8821 と同一パターン。

| # | ユーザー入力 | 応答（抜粋） | E2E (ms) | LLM 判定 |
|---|-------------|-------------|----------|----------|
| 1 | ロキソニンって眠くなる？ | sage_qa（プレースホルダ） | 8,706 | 🟢 |
| 2 | ロキソニンって眠くなる？ | 同上本文 | 8,702 | 🟢 |

**LLM コスト**: ¥0.0956（triage stage1 のみ）

---

## 優先アクション

| 優先度 | アクション | 根拠 |
|--------|-----------|------|
| **P0** | **`side_effect_qa_mishandled` ヒューリスティック修正** — shadow `sub_route` フォールバック、`medicine_qa`/`sage_qa` 除外、`looks_escalation` から標準免責除外 | 本窓口 2/2 が false positive。前日 critical 判定との整合 |
| **P0** | **01:34 UTC スキャン burst 向け ALB WAF ルール**（`.env` / `.git` / `phpinfo`） | 267 件 / 2 分集中 |
| **P1** | **`/helth` → `/health`** 外部モニタ修正 | 6 件 typo 404 |
| **P1** | **`turns[].routing.sub_route` へ shadow 結果伝播** + triage subcategory enum 統一（`drug_side_effect` / `side_effects`） | 分析精度・ヒューリスティック精度向上 |
| **P1** | **triage 非 LLM 区間（~1.9 s）の内部計測追加** | stage1 LLM ~2 s に対し triage 全体 ~4 s |
| **P2** | CloudWatch ERROR アラートから Gunicorn SIGTERM 除外 | ERROR 4 = SIGTERM 4 |
| **P2** | Neon `DATABASE_URL` から `channel_binding=require` 削除 | 起動 WARNING ノイズ |
| **P2** | `analyze_aws_logs.py` で Polly outbound `/v1/speech` を HTTP 4xx 集計から除外 | 誤分類 2 件 |
| **P2** | 前日 escalation bug（24h レポート）と本窓口 ok の**再現条件差分**を調査 | デプロイ後改善か入力表現依存か |

---

## 付録

| 種別 | パス |
|------|------|
| 生ログ | `log/raw/downloaded-aws-logs-20260725-20260725-20260725-024329.json` |
| セクション JSON | `log/analysis/downloaded-aws-logs-20260725-20260725-20260725-024329/sections/` |
| セッション transcript | `log/analysis/downloaded-aws-logs-20260725-20260725-20260725-024329/sessions/` |
| Draft | `log/analysis/downloaded-aws-logs-20260725-20260725-20260725-024329/draft_*.md` |
| 前回レポート（24h） | `log/analysis/2026-07-25_downloaded-aws-logs-20260724-20260725-20260725-005002.md` |

**解析構成**: Wave A 4 エージェント（infra_errors / performance_cost / conversation_quality / integrations）+ Wave B 2 セッションエージェント。
