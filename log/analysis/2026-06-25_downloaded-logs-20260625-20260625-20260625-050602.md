# GCP Log Analysis Report

## メタデータ

| 項目 | 値 |
|------|-----|
| ソースファイル | `log/raw/downloaded-logs-20260625-20260625-20260625-050602.json` |
| 環境 (service) | **medicine-recommend-dev**（開発） |
| 期間 | 2026-06-25T04:12:50Z 〜 2026-06-25T05:05:54Z（約 53 分・差分エクスポート） |
| エントリ数 | 1,417 |
| 主な revision / commit | `medicine-recommend-dev-00123-bpf` / `a7455d2` |
| 前回カバレッジ終端 | 2026-06-25T04:13:20Z（`downloaded-logs-20260625-131332`） |

## エグゼクティブサマリー

本ウィンドウは前回エクスポート直後の **差分期間**（約 53 分）であり、**ユーザー会話・チャット処理は 0 件**。トラフィックのほぼ全てが管理 UI のセッション一覧ポーリング（`GET /api/sessions` / `PATCH /api/sessions/activity`）に限定された。

インフラ面では HTTP 4xx/5xx・ERROR ログともに **0 件**。04:59:22Z に Gunicorn ワーカー再起動が 1 回発生したが、同一リビジョン内の良性イベントであり、前後のリクエストはすべて 200。Neon PostgreSQL も再起動後に正常初期化。

`PIPELINE_PERF`・LLM コスト・LINE Webhook・会話セッションはいずれも **0**。会話品質・チャット性能の評価は本ウィンドウでは不可。直前の長時間ログ（`131332`）の所見を上書きするデータではない。

**即時対応必須のインシデントは検出されず。**

---

## インフラ・エラー（infra_errors）

### HTTP 異常

| 指標 | 値 |
|------|-----|
| HTTP 4xx/5xx | **0 件** |
| status 分布 | 200: 672 / 204: 26 |
| 5 秒以上の slow endpoint | 0 |
| text_errors | 0 |

**Severity**: 🟢 info

### テキスト ERROR

`ERROR` / `CRITICAL` / `WARNING` は **0 件**。severity は `INFO` / `DEFAULT` のみ。

### デプロイ境界

| 時刻 (UTC) | イベント |
|------------|----------|
| 2026-06-25T04:12:50Z | リビジョン `medicine-recommend-dev-00123-bpf` 稼働開始（Cloud Build 自動デプロイ） |

窓全体が単一リビジョン。ロールアウト中の旧インスタンス SIGTERM は本エクスポート範囲外の可能性あり。

### Gunicorn ワーカー再起動（良性）

| 時刻 (UTC) | イベント |
|------------|----------|
| 04:59:22Z | Worker exiting (pid: 2) → Booting worker (pid: 66) |
| 04:59:30Z | Application startup complete（約 7.5 秒） |

- HTTP 503: **0 件**
- 再起動ウィンドウ (04:59:20〜04:59:35) のリクエスト 6 件: すべて **200**
- `[Errno 9] Bad file descriptor` は shutdown 時の既知ノイズ

**Severity**: 🟢 info

---

## 性能・コスト（performance_cost）

### PIPELINE_PERF

| 指標 | 値 |
|------|-----|
| `pipeline_perf_count` | **0** |
| `llm_call_count` | **0** |
| `total_cost_jpy` | **0.0** |

チャット/LINE 応答がないため計測対象なし。**N/A**。

### HTTP レイテンシ（管理 UI ポーリング）

| エンドポイント | n | p50 | p95 | max |
|----------------|---|-----|-----|-----|
| `GET /api/sessions` | 619 | 293 ms | **589 ms** | 881 ms |
| `PATCH /api/sessions/activity` | 79 | 296 ms | **702 ms** | 736 ms |

04:59:24Z の 881 ms はワーカー再起動直後の 1 回性スパイク。チャット応答 SLA とは別 KPI。

### 参考（前ログ `131332`）

同一リビジョンで Web/LINE `PIPELINE_PERF` P95 ~9 秒・LLM 0.57 円/10 ターンが観測済み。本差分では改善/悪化の判定不可。

---

## 会話品質（conversation_quality）

### セッション総合評価サマリー

| session_id | channel | ターン数 | 総合 grade | critical | warning | 要約 |
|------------|---------|----------|------------|----------|---------|------|
| — | — | 0 | — | 0 | 0 | **会話セッションなし** |

`chat_flow.trace_count=0`、`counseling_detail` も生ログで 0 件。パーサ欠落ではなく **実トラフィック不在**。

### セッション別 — 会話 transcript

**対象セッション 0 件** — Wave B スキップ。

前回エクスポート（`131332`）で Web Concierge + LINE emoji テストが観測された後の **アイドル期間**の差分ログ。

### 意図ずれ・品質問題

| 時刻 | session_id | ユーザー入力 | issue_type | 深刻度 | 原因仮説 |
|------|------------|--------------|------------|--------|----------|
| — | — | — | — | — | 評価対象ターン 0 |

### 遅い trace（≥8s）

該当なし（`trace_count=0`）。

---

## 連携・その他（integrations）

### LINE webhook

| 指標 | 値 |
|------|-----|
| Webhook リクエスト | **0 件** |
| テキストメッセージ | **0 件** |

前回長窓（`131332`）では 12 件（すべて 200）が観測。本窓は LINE 利用なしの区間。

### DB / Neon

| 時刻 (UTC) | イベント |
|------------|----------|
| 04:59:26Z | PostgreSQL connection pool created (min: 2, max: 10) |
| 04:59:28Z | Database tables initialized successfully |
| 04:59:29Z | Database initialized successfully |

接続失敗・タイムアウトなし。ワーカー起動から DB 初期化完了まで約 4 秒。

### その他シグナル

- `job_lock_events` 2 件は Gunicorn ライフサイクルログの誤分類（`LINE_LOCK_KEYWORDS` の `"waiting for"` マッチ）。`LineJobLock` イベントではない。
- OpenAI / 外部 API エラー: なし

---

## 推奨アクション

1. 🟢 **即時対応不要** — infra・統合・会話品質いずれもインシデント相当なし
2. 🟢 **会話品質評価は前ログ `131332` を参照** — 本差分はアイドル期間。テスト実施後の incremental fetch で `trace_count > 0` を確認
3. 🟡 **（任意）エクスポートフィルタ最適化** — `/api/sessions` ポーリングがログの大半を占める。差分解析の SNR 向上のため `scripts/export_gcp_logs.py` で admin パス除外を検討
4. 🟡 **（任意）CLI パーサ改善** — `job_lock_events` の Gunicorn ライフサイクル誤分類を `gcp_cloud_run_log_parser.py` で修正
5. 🟢 **（任意）非チャット API レイテンシ集計** — 会話ゼロウィンドウでも `performance_cost` セクションが空にならないよう `analyze_gcp_logs.py` に `httpRequest` ベース集計を追加

---

## 付録

- 抽出: `log/analysis/downloaded-logs-20260625-20260625-20260625-050602/`
- manifest: `manifest.json`
- Wave A drafts: `draft_infra_errors.md`, `draft_performance_cost.md`, `draft_conversation_quality.md`, `draft_integrations.md`
- 前回会話ありエクスポート: `log/analysis/downloaded-logs-20260625-131332/`
