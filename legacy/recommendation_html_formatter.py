"""
Legacy recommendation HTML assembly (read-only archive).

The active legacy HTML builder lives in ``chat_recommendation_flow.py`` (non-Sage branch).
Sage Web uses ``build_diagnosis_v1()`` + ``sage_reco`` marker instead.

Do not add new call sites — extend diagnosis v1 builders for Sage UI.
"""
