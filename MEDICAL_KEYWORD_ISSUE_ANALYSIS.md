# 医療関連キーワード検出の問題分析

## 問題の概要

ユーザーが医療関連キーワードのリストから多くのキーワードを削除したため、以下の症状が検出されなくなっています：

- 肩がこります → 「肩」「こり」が削除されたため検出不可
- 痰が絡みます → 「痰」「たん」が削除されたため検出不可
- 寒気がします → 「寒気」が削除されたため検出不可
- 胸やけがします → 「胸やけ」が削除されたため検出不可
- 湿疹が出ています → 「湿疹」が削除されたため検出不可
- 打撲しました → 「打撲」が削除されたため検出不可
- 捻挫しました → 「捻挫」が削除されたため検出不可
- 目が疲れます → 「目」「疲れ」が削除されたため検出不可
- 眠れません → 「眠」が削除されたため検出不可
- 疲れが取れません → 「疲れ」が削除されたため検出不可
- イライラします → 「イライラ」が削除されたため検出不可
- 不安です → 「不安」が削除されたため検出不可

## 本番環境への影響

### 影響があるケース（10文字未満の入力）

**コード（2765行目）:**
```python
if not has_medical_keyword and len(user_text_stripped) < 10:
    return {
        "status": "error",
        "reason": "症状を入力してください",
        ...
    }
```

**影響:**
- 10文字未満の入力で医療キーワードが検出されない場合、**推奨処理が停止**されます
- 例：「肩がこります」（7文字）→ エラーを返して推奨されない
- 例：「痰が絡む」（5文字）→ エラーを返して推奨されない

### 影響が少ないケース（10文字以上の入力）

**コード（2765行目）:**
```python
if not has_medical_keyword and len(user_text_stripped) < 10:
    logger.warning(...)  # 警告のみ
    return {...}  # エラーを返す
```

**影響:**
- 10文字以上の入力の場合は、警告を出すだけで処理は継続されます
- NLUで症状が検出されれば、推奨処理は正常に実行されます
- 例：「肩がこります。痛みがあります。」（15文字）→ 警告は出るが推奨処理は継続

## 問題の原因

ユーザーが以下のキーワードを削除したため：

**削除されたキーワード:**
- 風邪関連: 「痰」「たん」「くしゃみ」「寒気」「悪寒」「関節」「筋肉」
- 胃腸関連: 「胸やけ」「胸焼け」「胃もたれ」「腹痛」「お腹」
- 皮膚関連: 「湿疹」「皮膚」「かぶれ」「炎症」
- 外傷関連: 「打撲」「打ち身」「捻挫」「くじいた」
- 筋肉・関節関連: 「肩こり」「肩」「腰痛」「腰」
- 目関連: 「目」「眼」「充血」「疲れ」
- 睡眠・精神関連: 「眠」「睡眠」「疲れ」「だるい」「倦怠」「イライラ」「不安」「ストレス」
- その他: 「生理」「月経」「歯」「耳」

## 解決策

### オプション1: キーワードリストを復元（推奨）

削除されたキーワードを復元して、すべての症状を検出できるようにする。

**メリット:**
- すべての症状が検出される
- 本番環境での推奨処理が正常に機能する

**デメリット:**
- キーワードリストが長くなる
- 誤検知の可能性が若干増える（ただし、10文字未満のチェックがあるため影響は限定的）

### オプション2: チェック条件を緩和

10文字未満の条件を削除または緩和する。

**メリット:**
- 短い入力でもNLUで症状が検出されれば推奨される

**デメリット:**
- 意味のない入力も通過する可能性がある
- セキュリティリスクが増える可能性

### オプション3: NLU結果に依存

医療キーワードチェックを削除し、NLU結果のみに依存する。

**メリット:**
- キーワードリストの保守が不要

**デメリット:**
- NLUが失敗した場合のフォールバックがない
- セキュリティリスクが増える可能性

## 推奨される対応

**オプション1（キーワードリストの復元）を推奨します。**

理由：
1. 本番環境で推奨処理が停止されるリスクを回避できる
2. 10文字未満のチェックがあるため、誤検知のリスクは限定的
3. 既存のセキュリティチェックを維持できる

## 修正コード例

```python
medical_keywords = [
    # 基本キーワード
    "痛", "熱", "咳", "鼻", "喉", "頭", "胃", "下痢", "便秘", "吐", "めまい",
    "かゆ", "発疹", "不眠", "疲労", "症状", "病気", "薬", "医", "病",
    # 風邪関連
    "痰", "たん", "くしゃみ", "寒気", "悪寒", "関節", "筋肉",
    # 胃腸関連
    "胸やけ", "胸焼け", "胃もたれ", "腹痛", "お腹",
    # 皮膚関連
    "湿疹", "皮膚", "かぶれ", "炎症",
    # 外傷関連
    "打撲", "打ち身", "捻挫", "くじいた",
    # 筋肉・関節関連
    "肩こり", "肩", "腰痛", "腰",
    # 目関連
    "目", "眼", "充血", "疲れ",
    # 睡眠・精神関連
    "眠", "睡眠", "疲れ", "だるい", "倦怠", "イライラ", "不安", "ストレス",
    # その他
    "生理", "月経", "歯", "耳"
]
```

