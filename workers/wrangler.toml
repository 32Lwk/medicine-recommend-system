# Cloudflare Worker — aws-staging-wake
# Deploy: cd workers && npm install && npx wrangler deploy
# Secrets (Dashboard or CLI): WAKE_API_URL, WAKE_TOKEN, ORIGIN_URL

name = "aws-staging-wake"
main = "src/index.js"
compatibility_date = "2024-08-01"
account_id = "2a1ac0678cd0b207ca4fa5681a9a0690"

routes = [
  { pattern = "aws.medicine.yutok.dev/*", zone_name = "yutok.dev" }
]
