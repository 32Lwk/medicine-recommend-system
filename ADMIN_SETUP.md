# 管理画面セットアップガイド

## 🔑 必須環境変数

Renderダッシュボードの「Environment」タブで以下を設定してください：

### 1. OpenAI APIキー（必須）
```
Key: OPENAI_API_KEY
Value: sk-... （あなたのOpenAI APIキー）
```

**取得方法**: https://platform.openai.com/api-keys

### 2. 管理画面パスワード（必須）
```
Key: ADMIN_PASSWORD
Value: 任意の強力なパスワード
```

**推奨**: 12文字以上、英数字記号を含む

### 3. Flaskシークレットキー（必須）
```
Key: SECRET_KEY
Value: ランダムな文字列（32文字以上推奨）
```

**生成方法**:
```bash
python -c "import secrets; print(secrets.token_hex(32))"
```

### 4. Flask環境設定（推奨）
```
Key: FLASK_ENV
Value: production
```

---

## 🔐 管理画面へのアクセス

### URL
```
https://medicine-recommend-system.onrender.com/admin
```

### 認証情報
- **ユーザー名**: `admin`
- **パスワード**: 環境変数 `ADMIN_PASSWORD` で設定した値

---

## 🧪 機能テスト手順

### 1. システム状況
1. 「システム状況」ボタンをクリック
2. 以下が表示されることを確認：
   - ✅ システム全体の状態
   - ✅ AI自動応答のON/OFF状態
   - ✅ CSVファイルの読み込み状況
   - ✅ パフォーマンス統計

### 2. AI管理
1. 「AI管理」ボタンをクリック
2. 「AI自動応答ON」または「OFF」をクリック
3. 右上に成功通知が表示されることを確認

### 3. 医薬品相談テスト
1. 「医薬品相談テスト」ボタンをクリック
2. 症状を入力（例：「頭が痛いです」「のどが痛い」）
3. 「送信」をクリック
4. 以下が表示されることを確認：
   - ✅ 抽出された症状
   - ✅ 医薬品タイプ
   - ✅ 推奨医薬品

**注意**: この機能を実行すると、パフォーマンス統計がカウントされます。

### 4. ログクリア
1. 「ログクリア」ボタンをクリック
2. 確認ダイアログで「OK」
3. 以下がクリアされることを確認：
   - ✅ すべてのセッション履歴
   - ✅ 手動返信待ちキュー
   - ✅ ネットワークログ
   - ✅ ログファイル

---

## ⚠️ トラブルシューティング

### エラー: "認証が必要です"
**原因**: パスワードが正しくない、または環境変数が設定されていない

**解決策**:
1. Renderダッシュボード → Environment タブ
2. `ADMIN_PASSWORD` が設定されているか確認
3. 正しいパスワードを入力

### エラー: "OpenAI APIキーが設定されていません"
**原因**: `OPENAI_API_KEY` 環境変数が設定されていない

**解決策**:
1. Renderダッシュボード → Environment タブ
2. `OPENAI_API_KEY` を追加
3. 保存して再起動を待つ（1〜2分）

### エラー: "通信エラーが発生しました"
**原因**: サーバーとの通信に失敗

**解決策**:
1. Renderの「Logs」タブでエラーログを確認
2. サービスが起動しているか確認
3. ネットワーク接続を確認

### パフォーマンス統計が0のまま
**原因**: まだ医薬品推奨が実行されていない

**解決策**:
1. ユーザー画面で症状を入力
2. または「医薬品相談テスト」を実行
3. 再度「システム状況」を開く
4. → 数値が更新されているはず

---

## 📊 レイアウト

### デスクトップ（768px以上）
```
┌─────────────────┬─────────────────────────┐
│ 🤖 AI管理      │                         │
│                │   📋 手動返信待ちキュー    │
├─────────────────┤                         │
│ 📊 統計情報     │   （大きく表示）          │
└─────────────────┴─────────────────────────┘
```

### スマホ（768px以下）
```
┌─────────────────┐
│ 🤖 AI管理      │
├─────────────────┤
│ 📊 統計情報     │
├─────────────────┤
│ 📋 手動返信     │
│   待ちキュー    │
└─────────────────┘
```

---

## 🎯 セキュリティ推奨事項

1. ✅ 強力なパスワードを設定（12文字以上）
2. ✅ APIキーを定期的に再生成
3. ✅ 管理画面へのアクセスを信頼できるネットワークのみに制限（必要に応じて）
4. ✅ Renderの環境変数でAPIキーを管理（コードにハードコード禁止）

---

**管理画面のセットアップ完了！** 🎉

