# スコアリングシステム詳細分析とC言語化による高速化効果

## 1. スコアリング処理の詳細分析

### 1.1 各項目のスコア計算方法

#### 1.1.1 症状適合度スコア（`calculate_symptom_match_score`）

**計算式**:
```python
症状スコア = Σ(症状辞書の重み) / 症状数
```

**処理内容**:
1. 効能効果テキストを正規化（NFKC正規化、小文字化、空白・記号除去）
2. 各症状について：
   - 症状名を正規化
   - 同義語リストから正規化した同義語を生成
   - 効能効果テキストに症状名または同義語が含まれるかチェック
   - 含まれる場合、症状辞書の重み（0.7-0.95）を加算
3. 症状数で割って平均化

**処理頻度**:
- 簡易スコアリング: 全候補（1,000-2,000件）× 症状数（平均2-3個）× 効能効果テキスト（1件）
- 詳細スコアリング: 選別候補（250-500件）× 症状数（平均2-3個）× 効能効果テキスト（1件）

**計算時間の見積もり**:
- 簡易スコアリング: 約150-300ms（全候補）
- 詳細スコアリング: 約50-100ms（選別候補）

**ボトルネック**:
- 文字列正規化処理（`normalize_text`）が最も時間がかかる
- 文字列マッチング処理（`in`演算子）も高頻度

#### 1.1.2 効能特異性スコア（`calculate_efficacy_specificity_score`）

**計算式**:
```python
特異性スコア = (症状マッチ数 / 症状数) × (1.0 - 長さペナルティ × 0.25)
最終スコア = 特異性スコア × 広域効能ペナルティファクター
```

**処理内容**:
1. 効能効果テキストを正規化
2. 症状名リストを正規化
3. 各症状が効能効果テキストに含まれるかチェック
4. マッチ数を症状数で割って特異性比率を計算
5. 効能効果の長さによるペナルティを適用（120文字を基準）
6. 広域効能（滋養強壮など）の場合、症状との整合性を確認してペナルティを適用

**処理頻度**: 症状適合度スコアと同じ

**計算時間の見積もり**:
- 簡易スコアリング: 約150-300ms
- 詳細スコアリング: 約50-100ms

#### 1.1.3 年齢適合性スコア（`calculate_age_fit_score`）

**計算式**:
```python
年齢スコア = f(年齢, 年齢制限)
- 15歳以上: 1.0
- 7-15歳: 0.7
- 7歳未満または15歳未満制限: 0.0（除外）
```

**処理内容**:
1. 年齢制限から最小年齢を抽出（正規表現）
2. ユーザーの年齢と比較
3. 適合度に応じてスコアを返す

**処理頻度**: 全候補（簡易・詳細両方）

**計算時間の見積もり**: 非常に短い（約10-20ms）

#### 1.1.4 用法簡便性スコア（`calculate_usage_convenience_score`）

**計算式**:
```python
服用回数によるスコア:
- 1日1回: 1.0
- 1日2回: 0.8
- 1日3回: 0.6
- 1日4回: 0.4
- それ以上: 0.2
```

**処理内容**:
1. 用法テキストから1日の服用回数を抽出（正規表現）
2. 服用回数に応じてスコアを返す

**処理頻度**: 選別候補のみ（詳細スコアリング）

**計算時間の見積もり**: 非常に短い（約5-10ms）

#### 1.1.5 副作用リスクスコア（`calculate_side_effect_risk_score`）

**計算式**:
```python
副作用リスクスコア = -1.0 ～ 0.0（負の値）
- 副作用レベル「高」: -0.8
- 副作用レベル「中」: -0.5
- 副作用レベル「低」: -0.2
- 禁忌条件に該当: 2倍（リスク倍増）
- 妊娠中・授乳中: 1.5倍
```

**処理内容**:
1. 成分リストを抽出（改行区切り）
2. 副作用データ（CSV）から該当成分を検索（`str.contains`）
3. 副作用レベルを数値に変換
4. 禁忌条件や妊娠・授乳状態を考慮してリスクを計算

**処理頻度**: 選別候補のみ（詳細スコアリング）

**計算時間の見積もり**: 約50-100ms（pandas DataFrame検索がボトルネック）

#### 1.1.6 相互作用リスクスコア（`calculate_interaction_risk_score`）

**計算式**:
```python
相互作用リスクスコア = -1.0 ～ 0.0（負の値）
- 相互作用レベル「高」: -0.8
- 相互作用レベル「中」: -0.5
- 相互作用レベル「低」: -0.2
```

**処理内容**:
1. 候補医薬品の成分リストを抽出
2. 現在服用中の薬との相互作用をチェック（相互作用データCSVから検索）
3. 相互作用レベルを数値に変換

**処理頻度**: 選別候補のみ（詳細スコアリング）

**計算時間の見積もり**: 約50-100ms（pandas DataFrame検索がボトルネック）

#### 1.1.7 基本スコアの計算

**計算式**:
```python
base_score = (
    SCORING_WEIGHTS["症状適合度"] × symptom_score +      # 0.35
    SCORING_WEIGHTS["効能特異性"] × efficacy_score +     # 0.25
    SCORING_WEIGHTS["年齢適合性"] × age_score +          # 0.20
    SCORING_WEIGHTS["用法簡便性"] × usage_score +        # 0.05
    SCORING_WEIGHTS["副作用リスク"] × side_effect_score + # -0.20
    SCORING_WEIGHTS["相互作用リスク"] × interaction_score # -0.10
)
```

**重み付け**:
- 症状適合度: 0.35（最も重要）
- 効能特異性: 0.25
- 年齢適合性: 0.20
- 用法簡便性: 0.05
- 副作用リスク: -0.20（負の値）
- 相互作用リスク: -0.10（負の値）

**処理時間の見積もり**: 非常に短い（数値計算のみ、約1-2ms）

#### 1.1.8 調整スコアの計算

**計算式**:
```python
adjustment_score = (
    symptom_specificity_penalty +     # 症状特異性ペナルティ（-0.30 ～ 0.0）
    risk_penalty +                    # リスク成分ペナルティ（-0.30 ～ 0.0）
    throat_bonus +                    # のど症状特化ボーナス（0.0 ～ +0.25）
    symptom_boost +                   # 症状特化型ブースト（-0.20 ～ +0.25）
    allergy_penalty +                 # アレルギーペナルティ（-0.20 ～ 0.0）
    allergy_boost                     # アレルギーブースト（0.0 ～ +0.20）
)
scaled_adjustment = max(-0.30, min(0.25, adjustment_score))
```

**処理時間の見積もり**: 非常に短い（数値計算のみ、約1-2ms）

#### 1.1.9 最終スコアの計算（非線形変換含む）

**計算式**:
```python
# 基本スコアの底上げ
if base_score < 0.45:
    adjusted_base_score = 0.5
elif base_score < 0.5:
    adjusted_base_score = 0.5 + (base_score - 0.45) * 0.5 / 0.05

# 非線形変換（高スコア領域の細かい差別化）
if adjusted_base_score >= 0.5:
    normalized_base = (adjusted_base_score - 0.5) / 0.23  # 0.5-0.73を0-1に正規化
    expanded_base = 0.5 + (normalized_base ** 0.85) * 0.23  # 平方根（0.85乗）
    total_score = expanded_base + scaled_adjustment
else:
    total_score = adjusted_base_score + scaled_adjustment

# 漢方薬の優先度調整
if 漢方薬 and not prefers_kampo:
    total_score *= 0.8
```

**処理時間の見積もり**: 非常に短い（数値計算のみ、約1-2ms）

### 1.2 正規化・非線形変換処理の詳細

#### 1.2.1 Min-Max正規化処理

**処理内容**（詳細スコアリング後の正規化）:
```python
# ステップ5.2.5: Min-Max正規化
raw_scores = [c.get('raw_score', 0.0) for c in candidates]
min_raw_score = min(raw_scores)
max_raw_score = max(raw_scores)
score_range = max_raw_score - min_raw_score

for candidate in candidates:
    raw_score = candidate.get('raw_score', 0.0)
    
    # 0.5以下のスコアは0.0にマッピング
    if raw_score <= 0.5:
        normalized_score = 0.0
    else:
        # Min-Max正規化: (raw_score - min) / (max - min)
        if score_range > 0:
            min_max_normalized = (raw_score - min_raw_score) / score_range
        else:
            min_max_normalized = 1.0 if raw_score > 0.5 else 0.0
        
        # 非線形変換（平方根）で差を拡大
        normalized_score = math.sqrt(min_max_normalized)
        normalized_score = min(1.0, max(0.0, normalized_score))
    
    candidate['final_score'] = normalized_score
```

**処理頻度**: 選別候補（250-500件）のみ

**計算時間の見積もり**:
- 最大値・最小値の計算: 約5-10ms
- 各候補の正規化: 約10-20ms（平方根計算含む）
- **合計**: 約15-30ms

**C言語化による高速化見積もり**:
- 平方根計算の高速化: 約3-5倍
- リスト操作の高速化: 約2-3倍
- **総合的な高速化**: 約2-3倍（15-30ms → 5-15ms）

#### 1.2.2 相対スコア化処理

**処理内容**:
```python
# ステップ5.4: 相対スコア化（最高スコアを100%として正規化）
if top_candidates:
    max_score = top_candidates[0].get('final_score', 0.0)
    if max_score > 0:
        for candidate in top_candidates:
            relative_score = candidate['final_score'] / max_score
            candidate['relative_score'] = relative_score
            # スコア帯の判定（高/中/低）
            if relative_score >= 0.9:
                candidate['score_level'] = '高'
            elif relative_score >= 0.7:
                candidate['score_level'] = '中'
            else:
                candidate['score_level'] = '低'
```

**処理頻度**: 上位候補（3-10件）のみ

**計算時間の見積もり**: 非常に短い（約1-2ms）

**C言語化による高速化見積もり**: 効果が限定的（処理時間が短すぎる）

### 1.3 推奨医薬品選定処理の詳細

#### 1.3.1 ソート処理

**処理内容**:
```python
# ステップ5.3: 詳細スコアリング結果をソート
candidates_sorted = sorted(candidates, key=lambda x: x['final_score'], reverse=True)
```

**処理頻度**: 選別候補（250-500件）

**計算時間の見積もり**: 約10-20ms（PythonのTimsort）

**C言語化による高速化見積もり**:
- クイックソート実装: 約2-3倍高速化
- **処理時間**: 約5-10ms

#### 1.3.2 特別ルールによる再ソート

**処理内容**:
1. 乗り物酔い薬の場合、スコア差が0.1以内なら指定第2類を優先
2. 肩こり・筋肉痛の場合、最適解の外用薬（フェイタス、バンテリン、サロンパス）を優先

**処理頻度**: 上位候補（3-10件）のみ

**計算時間の見積もり**: 約5-10ms

**C言語化による高速化見積もり**: 効果が限定的（処理時間が短すぎる）

#### 1.3.3 成分多様性の確保

**処理内容**（`ensure_ingredient_diversity`）:
```python
def ensure_ingredient_diversity(candidates: List[Dict], top_n: int = 3):
    # 主要成分が重複しすぎないように候補を再選別
    # 液剤を最初に1件確保（剤形多様性）
    # 成分の類似度を計算（集合の交差）
    # 類似度が閾値以上の場合、候補から除外
```

**処理頻度**: 上位候補（10-50件）のみ

**計算時間の見積もり**:
- 成分抽出: 約20-50ms（正規表現、文字列分割）
- 集合操作（交差・類似度計算）: 約10-20ms
- **合計**: 約30-70ms

**C言語化による高速化見積もり**:
- 文字列処理の高速化: 約5-7倍
- 集合操作の高速化: 約3-5倍
- **総合的な高速化**: 約4-6倍（30-70ms → 5-15ms）

## 2. C言語化による高速化効果の見積もり

### 2.1 正規化・非線形変換処理のC言語化

**対象処理**:
1. Min-Max正規化処理
2. 平方根計算（非線形変換）
3. 相対スコア化処理

**現在の処理時間**: 約15-30ms（選別候補250-500件）

**C言語化後の見積もり**: 約5-15ms

**高速化倍率**: 約2-3倍

**実装工数**: 約8-16時間（比較的容易、数値計算のみ）

### 2.2 推奨医薬品選定処理のC言語化

**対象処理**:
1. ソート処理（クイックソート実装）
2. 成分多様性の確保処理（文字列処理、集合操作）

**現在の処理時間**: 約40-90ms（選別候補250-500件）

**C言語化後の見積もり**: 約10-20ms

**高速化倍率**: 約4-6倍

**実装工数**: 約16-32時間（文字列処理と集合操作の実装が複雑）

### 2.3 全体の高速化効果

**対象処理の合計時間（現在）**:
- 正規化・非線形変換: 約15-30ms
- 推奨医薬品選定: 約40-90ms
- **合計**: 約55-120ms

**C言語化後の見積もり**:
- 正規化・非線形変換: 約5-15ms（2-3倍高速化）
- 推奨医薬品選定: 約10-20ms（4-6倍高速化）
- **合計**: 約15-35ms

**総合的な高速化**: 約3-4倍（55-120ms → 15-35ms）

**全体レスポンス時間への影響**:
- 現在の全体レスポンス時間: 約2,000-3,000ms
- 削減時間: 約40-85ms
- **全体レスポンス時間への影響**: 約1.5-3%高速化

**結論**: 正規化・非線形変換処理と推奨医薬品選定処理のC言語化による効果は限定的（全体レスポンス時間の約2-3%のみ）

## 3. スコアリングシステムの最適性の考察

### 3.1 スコアリングシステムの設計思想

#### 3.1.1 多面的な評価基準

現在のシステムは以下の6つの基準で医薬品を評価しています：

1. **症状適合度（0.35）**: 最も重要。ユーザーの症状と医薬品の効能の適合度
2. **効能特異性（0.25）**: 症状に特化した医薬品を優先
3. **年齢適合性（0.20）**: 年齢制限を考慮
4. **用法簡便性（0.05）**: 服用回数が少ないものを優先（影響は小さい）
5. **副作用リスク（-0.20）**: 副作用リスクが高いものを減点
6. **相互作用リスク（-0.10）**: 薬物相互作用を考慮

**評価**: 多面的な評価基準で、症状適合度を最重視しつつ、安全性も考慮している点は優れています。

#### 3.1.2 調整スコアによる細かい調整

調整スコアにより、以下の細かい調整が可能：

1. **症状特異性ペナルティ**: 単一症状時に複合薬にペナルティ
2. **リスク成分ペナルティ**: 高リスク成分含有時に減点
3. **症状特化型ブースト**: 症状に特化した医薬品にボーナス
4. **アレルギー関連**: アレルギー症状検出時の風邪薬ペナルティと鼻炎用薬ブースト

**評価**: 非常に細かい調整が可能で、専門的な知識が反映されています。

#### 3.1.3 非線形変換による差別化

1. **基本スコアの底上げ**: 低スコア領域（0.45未満）を0.5に底上げ
2. **非線形変換**: 高スコア領域（0.5以上）で平方根（0.85乗）を使用して細かい差別化
3. **Min-Max正規化**: 選別候補間での相対評価

**評価**: 非線形変換により、推奨される医薬品の多くが0.7-0.98の範囲に収まるように設計されており、適切な差別化が行われています。

### 3.2 システムの最適性評価

#### 3.2.1 強み

1. **多面的な評価**: 6つの基準で多面的に評価
2. **安全性の重視**: 副作用リスクと相互作用リスクを考慮
3. **細かい調整**: 調整スコアによる専門的な知識の反映
4. **適切な差別化**: 非線形変換により適切なスコア分布を実現
5. **二段階スコアリング**: 処理時間を約85%削減しながら精度を維持

#### 3.2.2 改善の余地

1. **重み付けの最適化**: 現在の重み付けは経験的なもの。機械学習による最適化の余地あり
2. **症状の重要度**: すべての症状を同等に扱っているが、症状の重要度を考慮する余地あり
3. **ユーザーフィードバック**: 推奨結果に対するユーザーのフィードバックを活用する余地あり
4. **動的重み付け**: 症状やユーザー属性に応じた動的な重み付けの余地あり

#### 3.2.3 最適性の総合評価

**評価**: ★★★★☆（4/5）

**理由**:
- 多面的な評価基準と安全性の重視は優れている
- 調整スコアによる細かい調整も適切
- 非線形変換により適切なスコア分布を実現
- ただし、重み付けの最適化やユーザーフィードバックの活用により、さらなる改善が可能

### 3.3 推奨される改善案

#### 3.3.1 短期的な改善（優先度: 高）

1. **重み付けの調整**: A/Bテストや専門家の意見を基に重み付けを調整
2. **症状の重要度**: 重症度に応じた症状の重要度を考慮
3. **ユーザーフィードバックの収集**: 推奨結果に対するユーザーの評価を収集

#### 3.3.2 中期的な改善（優先度: 中）

1. **機械学習による最適化**: ユーザーフィードバックを基に重み付けを最適化
2. **動的重み付け**: 症状やユーザー属性に応じた動的な重み付け
3. **説明可能性の向上**: スコアの内訳をユーザーに分かりやすく表示

#### 3.3.3 長期的な改善（優先度: 低）

1. **深層学習モデルの導入**: より複雑なパターンを学習
2. **パーソナライゼーション**: ユーザーの過去の選択を基にパーソナライズ

## 4. 結論

### 4.1 C言語化による高速化効果

正規化・非線形変換処理と推奨医薬品選定処理のC言語化による効果は限定的：

- **処理時間の削減**: 約40-85ms
- **全体レスポンス時間への影響**: 約1.5-3%高速化
- **実装工数**: 約24-48時間

**推奨**: 優先度は低い。より効果的な最適化（キャッシュ拡張、ChatGPT API最適化）を優先すべき。

### 4.2 スコアリングシステムの最適性

現在のシステムは多面的な評価基準、安全性の重視、細かい調整、適切な差別化により、優れた設計となっています。

**推奨**: 
- 短期的には重み付けの調整と症状の重要度の考慮
- 中期的には機械学習による最適化とユーザーフィードバックの活用

---

**作成日**: 2025年1月
**対象システム**: チャット型医薬品相談ツール
**分析者**: AI Assistant

